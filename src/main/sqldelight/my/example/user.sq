CREATE TABLE user (
    name TEXT NOT NULL PRIMARY KEY,
    pass TEXT NOT NULL,
    money INTEGER NOT NULL DEFAULT 0
);

INSERT INTO user(name, pass, money) VALUES
('vader', 'qwerty', 1000000000),
('luke', '12345', 3000000),
('leia', '12345', 200000000),
('solo', '111', 10000),
('chewbacca', '', 0);

money:
SELECT money FROM user WHERE name=?;

pass:
SELECT pass FROM user WHERE name=?;

minus:
UPDATE user SET money = money-? WHERE name=?;
